/**
 * Artist profile row 2 (desktop): how many of 8 equal-width grid tracks the
 * interview uses; biography fills the rest (8 minus this value).
 *
 * - 4 → 50% interview (same as old 2-of-4 columns)
 * - 5 → 62.5% interview (between 2 and 3 of the old 4-col layout)
 * - 6 → 75% interview (same as old 3-of-4)
 */
export const ARTIST_INTERVIEW_GRID_TRACKS: 4 | 5 | 6 = 5;
